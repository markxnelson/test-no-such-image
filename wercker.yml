box: ubuntu 
no-response-timeout: 60

build:
  steps:
    - script:
      code: | 
        echo $WERCKER_GIT_COMMIT
    - internal/docker-push:
        disable-sync: true
        repository: mark
        tag: $WERCKER_GIT_COMMIT
test:
  box: mark:$WERCKER_GIT_COMMIT
  steps:
    - script:
      code: | 
        echo $WERCKER_GIT_COMMIT
