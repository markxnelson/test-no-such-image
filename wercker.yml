box: ruby:2.3 
no-response-timeout: 60

build:
  services:
    - id: mysql:5.6
      env:
        MYSQL_ROOT_PASSWORD: foobar
  steps:
    - script:
      code: | 
        echo $WERCKER_GIT_COMMIT
test:
  steps:
    - script:
      code: | 
        echo $WERCKER_GIT_COMMIT
